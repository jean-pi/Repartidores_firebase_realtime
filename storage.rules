


rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /avatarUsers/{fileName} {
      // Permitimos lectura a todos (para ver el avatar)
      allow read: if true; 
      // Permitimos escritura solo si el usuario está autenticado
      allow write: if request.auth != null;
    }
  }
}