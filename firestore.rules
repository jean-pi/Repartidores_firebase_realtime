service cloud.firestore {
  match /databases/{database}/documents {
    match /repartidoresCollection/{document=**} {
      // Solo usuarios logueados pueden leer o editar repartidores
      allow read, write: if request.auth != null;
    }
  }
}
